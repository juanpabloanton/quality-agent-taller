{
  "mcpServers": {
    "semgrep": {
      "command": "uvx",
      "args": [
        "--python", "3.13",
        "--with", "setuptools<81",
        "--from", "semgrep",
        "semgrep", "mcp"
      ],
      "env": {
        "SEMGREP_APP_TOKEN": "${SEMGREP_APP_TOKEN}"
      }
    }
  }
}
